[Unit]
Description=Mastodon Follow/Like Bot (docker-compose)
After=docker.service
Requires=docker.service

[Service]
Type=simple
WorkingDirectory=/home/michael/SynologyDrive/Python Projects/mastodon_like_follow_bot
ExecStart=/usr/bin/docker-compose up --build
ExecStop=/usr/bin/docker-compose down
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
